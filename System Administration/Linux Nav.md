Everythingf is a file
`Dir` / `folder` - special files
`dir` -> can contain files and other directories 
`root`-> top directory -> admins 

`/` - used as seperator for directories

Standard file hierarchy but may differ from different distros 
(we are using redhat)

root 
- etc
- home
	- users1
	- userN
- usr
	- lib
	- sbin
- dev
- kernel